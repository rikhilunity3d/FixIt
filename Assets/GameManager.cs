using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameManager : MonoBehaviour
{
    // Score Coins will be store in GameDataSO because coins are common for all
    // the levels so we will store it in GameDataSO were all LevelDataSO will be
    // store uncomment lines of LevelDataSO variable.



    [SerializeField]
    int scoreCoins;
    /*
    [SerializeField];
    LevelDataSO levelDataSO;
    */

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    
}
